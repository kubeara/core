export enum DeploymentType {
  PLATFORM_SERVICE = "PLATFORM_SERVICE",
  CUSTOM_SERVICE = "CUSTOM_SERVICE",
}

export enum DeploymentMode {
  CUSTOM_COMPOSE = "custom-compose",
  COMPOSE = "compose",
}
